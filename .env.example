# 复制此文件为 .env.local 并按需修改。
#
# 设置后端地址即可让前端连接 FastAPI 后端；
# 留空或删除该变量，则前端使用内置的 localStorage 离线演示模式。
#
# ⚠️ 注意本项目有两套互相独立的数据库：
#   - http://localhost:8080 → Docker 路线（docker compose），数据在 Docker 卷
#     physics_lab_db-data。与线上部署方式一致，**推荐**。
#   - http://localhost:8000 → 直接跑 uvicorn 的路线，数据在 backend/physics_lab.db。
# 两者互不可见。连错地址会出现"我做的数据不见了"的假象，改前先确认你要哪一份。
#
# 另有一个特殊值 "/"：表示与前端同源（请求走 /api/... 相对路径，由 nginx 同源代理，
# 无 CORS）。服务器部署推荐用它——同一个构建产物在 IP、域名、localhost 下都能用，
# 之后换域名或加 HTTPS 都不需要重新构建。
VITE_API_BASE_URL=http://localhost:8080
